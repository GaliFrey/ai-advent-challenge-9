[project]
name = "ai-advent-day-17"
version = "0.1.0"
description = "Local Datex documentation MCP server and agent"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28,<1",
    "mcp>=2.2,<3",
    "python-dotenv>=1.2,<2",
    "textual>=8.2,<9",
]

[tool.uv]
package = false
