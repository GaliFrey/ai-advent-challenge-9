[project]
name = "ai-advent-day-15"
version = "0.1.0"
description = "Controlled state transitions for a staged LLM assistant"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.3",
    "textual>=8.2.8",
]

[tool.uv]
package = false
