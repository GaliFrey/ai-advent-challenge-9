[project]
name = "ai-advent-day-18"
version = "0.1.0"
description = "Scheduled SSH login collection and read-only MCP tools"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28,<1",
    "mcp>=2.2,<3",
    "python-dotenv>=1.2,<2",
    "textual>=8.2,<9",
]

[tool.uv]
package = false
