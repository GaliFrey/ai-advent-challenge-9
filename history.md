# История изменений

## 17.09.2026 18:44

- День 14 подготовлен и опубликован полностью: видео реального прогона двух профильных сессий загружено на Яндекс Диск, публичные метаданные подтверждают MP4, размер 4 222 743 байта и чистый антивирусный статус; удалённые размер и MD5 совпали с локальными.
- README дополнен фактическим результатом пяти API-ходов, выводами, ограничениями и публичной ссылкой. Перед публикацией прошли 28 unit/headless-тестов и `uv lock --check`; в Git включены только файлы дня 14 и связанная документация, без видео, локальных сессий, audit и секретов.

## 17.09.2026 18:13

- В дне 14 удалена смысловая regex-проверка пользовательских запросов: теперь каждый ход обрабатывается одним вызовом `deepseek-v4-flash`, который возвращает строгий JSON с решением, доказательствами нарушений и пользовательским ответом. Код проверяет структуру, согласованность решения и допустимость ID, а детерминированно контролирует только наблюдаемые свойства готового ответа.
- Решения и ID нарушений сохраняются в метаданных сессии, поэтому отчёт больше не зависит от формулировки отказа; для старых сессий сохранено совместимое чтение. Добавлены проверки неизвестных ID, противоречивого решения, смешанной старой и новой истории; прошли 28 локальных unit/headless-тестов без реального API-вызова.

## 17.09.2026 18:03

- Повторная проверка фактической сессии дня 14 выявила два узких шаблона: форма `напишем` не попадала под precheck `напиши`, а естественный отказ DeepSeek `Не могу выполнить… [TECH-CODE-001]` не совпадал со строгим `ОТКАЗ · ID`. Precheck расширен на словоформы, а policy-отказ теперь распознаётся по явной формулировке отказа и валидному ID.
- Для текущей `session-01` повторно проверены фактические сообщения: отчёт вычисляет `TECH-CODE-001 · 1`. Добавлены регрессионные тесты точной пользовательской формулировки и естественного отказа; все 24 теста прошли.

## 17.09.2026 17:50

- По фактическим data дня 14 исправлен отчёт policy engine: первый запрос на Go был верно отклонён детерминированным precheck без API, а отказ DeepSeek на Python 3.10 ошибочно учитывался как `ALLOWED`. Явный отказ модели теперь даёт `REFUSED` и `VIOLATION`, а Python ниже 3.12 дополнительно ловится локально.
- Вкладка `Проверка` показывает как последний ход, так и счётчик отказов всей сессии; для фактической `session-01` повторно вычислено `TECH-CODE-001 · 2`. Базовая system-инструкция и служебные заголовки переведены на русский; все 23 локальных теста прошли.

## 17.09.2026 17:34

- Для `Russian tech lead` добавлен инвариант `TECH-CODE-001`: программные решения и примеры кода разрешены только на Python 3.12+, shell-команды допустимы. Явный запрос решения на другом языке отклоняется до API-вызова, а иноязычный fenced-блок в ответе модели блокируется post-check.
- Набор техлида получил версию `tech-lead-v2`; файл `v1` сохранён. При восстановлении сесии загружается закреплённая за ней версия, поэтому старые сессии остаются воспроизводимыми. Добавлены три регрессионных проверки; все 20 тестов прошли.

## 17.09.2026 17:28

- Восстановление чата дня 14 после переключения сессии переведено на ту же функцию Rich Text, что и вывод новых реплик. Метки пользователя и ассистента теперь сохраняют голубой и зелёный цвета и жирное начертание. Добавлен регрессионный тест; все 17 тестов прошли.

## 17.09.2026 17:07

- Исправлена привязка профиля в интерфейсе дня 14: выбор другого профиля теперь меняет профиль и hash инвариантов пустой сессии. Если в сессии уже есть диалог, для нового профиля автоматически создаётся отдельная сессия, а прежняя история сохраняется. Добавлены два headless-теста; все 16 тестов прошли.

## 17.09.2026 16:57

- По скриншоту реального терминала исправлена верхняя панель дня 14: убран ненужный выбор модели, а профиль, создание сессии, текущая сесия и автодемо получили фиксированные ширину и высоту без переносов. Кнопка отправки также получила явный размер.
- Модель зафиксирована на `deepseek-v4-flash` и отображается в строке контекста. Headless-тест теперь фиксирует одинаковую вертикальную позицию, высоту и помещение всех шести элементов панели в окне; все 14 тестов прошли.

## 17.09.2026 16:38

- День 14 реализован как самостоятельный `uv`-проект с Textual-чатом: `Russian tech lead` и `English tutor` имеют разные фиксированные наборы инвариантов. Профиль и версия/hash набора закрепляются за новой сессией; конфигурация, диалог и append-only audit хранятся раздельно.
- Явные конфликты перехватываются до API-вызова, а сгенерированный ответ проходит post-check; нарушающий кандидат заменяется объяснённым отказом. В интерфейсе видны активные правила, PASS/VIOLATION, фактический prompt и история разных сессий.
- Прошли 14 локальных unit/headless-тестов, включая разные решения для одного запроса, отказ без сети, блокировку нарушающего ответа, атомарное хранение и hash-защиту. Реальный API-запуск и визуальная проверка в пользовательском терминале не выполнялись.

## 17.09.2026 15:19

- День 13 подготовлен к публикации: итоговый реальный прогон завершился статусом `done` после одной доработки и повторной validation без ошибок стадий; README дополнен фактическими метриками, выводами и публичной ссылкой на видео.
- Перед публикацией прошли 24 unit/headless-теста, компиляция, `uv lock --check` и `git diff --check`. Загруженное MP4-видео проверено по размеру 91 131 838 байт и MD5 `3cab28dd5c3dea3d61148e74a301f1c9`, совпадающим с локальным файлом и публичными метаданными Яндекс Диска.

## 17.09.2026 16:43

- Фактический validation-вызов после исправления параметров дошёл до генерации, но завершился по `length`: GPT потратил весь общий лимит 1800 output tokens, включая reasoning, и успел вернуть только часть JSON. Для validation введён отдельный лимит 4000 и `reasoning.effort=low`; DeepSeek сохраняет лимит 1800.
- TUI теперь отрисовывает prompt до сетевого ожидания и показывает незавершённый ход `модель работает…`, фактическую модель, состояние `API: выполняется` и заголовок `PROMPT · ОТПРАВЛЕН`. После ответа pending-turn заменяется сохранённым результатом или ошибкой.

## 17.09.2026 16:27

- Конкретная ошибка OpenRouter `No endpoints found that can handle the requested parameters` сопоставлена с актуальным Models API: `openai/gpt-5.6-sol` поддерживает structured outputs и `max_tokens`, но не `temperature`.
- `temperature=0.0` теперь отправляется только в DeepSeek-вызовы; validation не содержит неподдерживаемый параметр, при этом `provider.require_parameters=true` сохранён для строгого выбора совместимого endpoint.

## 17.09.2026 16:18

- Второй пробный validation-вызов дошёл до OpenRouter с канонической моделью и получил HTTP 200, но прежний строгий parser скрыл несовпадение структуры за общим сообщением. Checkpoint остался на validation без повторения выполненных этапов.
- Разбор ответа поддерживает как строковый `message.content`, так и текстовые content-блоки, а usage — в Chat Completions и input/output вариантах. Встроенная ошибка или refusal распознаются отдельно; прочие нарушения контракта теперь сохраняют конкретную безопасную причину.

## 17.09.2026 16:02

- По фактическому журналу диагностирован HTTP 404 на validation: в OpenRouter отправлялся URL страницы модели `openai/gpt-5.6-sol-20260709` вместо канонического API slug `openai/gpt-5.6-sol`. Значение исправлено в коде, документации и `.env.example`; старое переопределение автоматически мигрируется при загрузке конфигурации.
- HTTP-ошибки теперь сохраняют короткое безопасное сообщение API вместе со статусом, если провайдер вернул JSON-описание. Checkpoint не продвигается и позволяет повторить только validation.

## 17.09.2026 15:47

- День 13 переведён на раздельные модели стадий: planning/execution выполняет выбранная основная модель DeepSeek, revision — `deepseek-v4-pro`, validation — закреплённая `openai/gpt-5.6-sol` через OpenRouter с отдельным ключом.
- Независимый валидатор использует строгую JSON Schema и обязан вернуть доказательные нарушения с конкретным исправлением и минимум одно неблокирующее улучшение. `passed` больше не принимается на веру от модели: приложение вычисляет его по отсутствию `issues` и сохраняет улучшения в checkpoint.
- В TUI добавлена явная карта моделей всех стадий и фактическая модель выбранного этапа. Диагностика записывает корректную модель каждого вызова; `.env.example` и README дополнены настройками OpenRouter без секретов.

## 17.09.2026 15:14

- Из технической панели всех стадий убрана дублирующая нижняя строка `API … · EVENTS … · path`: токены уже показаны в состоянии стадии, а путь не требовался для работы.
- Устранена неоднозначность интерфейса между `EVENTS` и `Ходов`: события журнала включают lifecycle-записи вроде `task_created`, тогда как ходы означают только фактические API-вызовы. Полный event-счётчик остаётся доступен в диагностическом отчёте.

## 17.09.2026 15:02

- Цепочка интерфейса дня 13 дополнена терминальной стадией `ЗАВЕРШЕНО`. Она становится активной при `phase=done`, показывает итоговый результат, оставшиеся замечания и общие input/output/total tokens, число вызовов и время всего прогона; пропущенная доработка отмечается отдельно.
- Quality gate расширен до двух попыток доработки: после каждого revision снова выполняется validation, успешная проверка завершает задачу сразу, а провал после второй доработки всегда переводит её в `done_with_issues` без возможности зациклиться.
- Техническая панель увеличена под полную итоговую статистику; добавлены проверки второго revision-checkpoint, завершения после двух неуспешных доработок и итогового экрана с общими токенами и результатом.

## 17.09.2026 14:39

- Компактная панель конфигурации дня 13 расширена до трёх строк: элементы по-прежнему занимают один ряд, но получили по одной строке вертикального воздуха сверху и снизу.

## 17.09.2026 14:31

- Верхняя панель конфигурации дня 13 сведена к одной строке: модель, workflow-профиль и задача используют компактный режим `Select`, а подписи и кнопки имеют высоту в одну терминальную строку без рамок.
- Headless-тест фиксирует одинаковую вертикальную координату и единичную высоту всех селекторов и кнопок панели.

## 17.09.2026 14:23

- Исправлено вертикальное обрезание строки цели и цепочки стадий: высота обоих контейнеров увеличена до семи строк с учётом рамки, padding и трёх строк содержимого.
- Headless-проверка теперь сравнивает доступную высоту содержимого контейнеров с фактической высотой поля цели и кнопки стадии, чтобы повторно не допустить скрытия подписей.

## 17.09.2026 14:16

- По результату визуального прогона исправлена компоновка TUI дня 13: цель задачи и кнопки запуска, паузы и продолжения собраны в одну компактную строку вместо отдельной высокой панели.
- Обновление состояния цепочки больше не стирает служебные классы Textual-кнопок; подписи стадий сохраняются, выбранная стадия получает внешний контур, а верхние элементы конфигурации имеют ограниченную ширину.
- Headless-тест теперь проверяет не только наличие элементов, но и одинаковую вертикальную координату поля цели и трёх кнопок действий.

## 17.09.2026 14:02

- Интерфейс дня 13 перестроен вокруг цепочки из четырёх рабочих стадий: план, выполнение, проверка и доработка. Текущая стадия имеет отдельную подсветку, а выбранную для просмотра можно переключать независимо.
- Для каждой стадии добавлен собственный чат из сохранённых фактических запросов и ответов; execution показывает отдельные ходы шагов, validation — обе проверки после рецикла. Справа отображаются состояние выбранной стадии, токены, время, параметры ответа и последний prompt.
- Обновлён headless-тест структуры и переключения стадий; прошли все 20 unit/headless-тестов.

## 17.09.2026 13:36

- Для структурированных стадий дня 13 включён нативный DeepSeek JSON mode через `response_format={"type":"json_object"}`; текстовые execution/revision-вызовы не изменены. Инструкции с явным словом JSON сохранены согласно контракту API, а локальная валидация полей и бизнес-правил остаётся обязательной.
- В диагностике каждого stage-turn теперь фиксируются выбранный формат ответа, лимит токенов и `finish_reason`. Тесты дополнены проверкой формата запросов planning, execution, validation и revision.
- Прошли 20 unit/headless-тестов, компиляция, `uv lock --check` и `git diff --check`; внешний API повторно не вызывался.

## 17.09.2026 13:18

- Разобран фактический пробный прогон дня 13: planning и первый execution-шаг прошли, второй execution-шаг остановился на сохранённом checkpoint с `finish_reason=length` при `DEEPSEEK_MAX_TOKENS=900`. Состояние осталось консистентным (`execution`, шаг 2, `failed`), поэтому повтор не требует заново выполнять planning и первый шаг.
- Лимит по умолчанию и локальное значение подняты до 1800 токенов; prompts ограничивают отдельный execution-результат 500 словами, а validation/revision — 900 словами. Незавершённый ответ теперь возвращается из API-разбора в оркестратор до формирования ошибки, поэтому журнал сохраняет его частичный текст, usage и длительность, не продвигая автомат.
- Убран пустой диагностический `profile_changed`, когда выбранный профиль фактически не менялся. Добавлен регрессионный тест `finish_reason=length`; прошли 20 unit/headless-тестов, компиляция, `uv lock --check` и `git diff --check`. Повторный реальный API-вызов не выполнялся.

## 17.09.2026 13:04

- Для дня 13 добавлен отдельный append-only журнал `data/runs/task-XX/events.jsonl`: он сохраняет полные prompts и ответы каждого stage-turn, модель, фазу/статус/шаг до и после вызова, переход, токены, длительность и безопасную ошибку. Отдельно фиксируются создание задачи, изменения цели и workflow-профиля, запрос и фактическая пауза, продолжение; HTTP-заголовки и API-ключ в журнал не попадают.
- Рабочий `task-XX.json` оставлен компактным checkpoint, а диагностическая история не перезаписывается после revision или повторной validation. Суммарные токены восстанавливаются в TUI после перезапуска; сбой журнала не откатывает уже сохранённое состояние и показывается как диагностическое предупреждение. JSONL-файлы данных добавлены в Git-ignore.
- Добавлена команда `report.py task-XX` со списком переходов и агрегатами вызовов, ошибок, времени, input/output/total tokens и разбивкой по стадиям. Прошли 19 unit/headless-тестов, компиляция, CLI-справка отчёта, `uv lock --check` и `git diff --check`; реальный API и визуальная проверка не выполнялись.

## 17.09.2026 12:42

- Validation дня 13 превращена в ограниченный quality gate: модель явно возвращает `passed`, замечания, инструкцию доработки и лучшую текущую версию. Первый провал переводит задачу в отдельную стадию `revision`, после чистого revision-turn выполняется повторная validation; успешная проверка даёт `done`, повторный провал — честный терминальный статус `done_with_issues`.
- В `TaskState` сохраняются решение и замечания валидатора, инструкция и полный результат доработки, а число автоматических доработок ограничено одной. Перезапуск между провалом validation и revision восстанавливает процесс из JSON; старые сообщения не передаются, новый prompt собирается только из формализованного состояния. Пауза поддержана и на стадии revision.
- Набор расширен до 17 тестов: проверены успешный рецикл после перезапуска, повторный провал без зацикливания, чистый двухсообщенческий контекст revision, передача исправленного результата повторному валидатору и сохранение revision-checkpoint. Повторно прошли компиляция, `uv lock --check` и `git diff --check`; реальный API и визуальный запуск не выполнялись.

## 17.09.2026 12:16

- День 13 реализован как самостоятельный `uv`-проект с Textual-интерфейсом и детерминированным автоматом `planning → execution → validation → done`. Состояние задачи атомарно хранит этап, текущий шаг, ожидаемое действие, исходную цель, план, результаты и последний фактический prompt; пауза сохраняет checkpoint, а продолжение не повторяет завершённые шаги и не требует нового объяснения цели.
- Созданы два новых workflow-профиля специально для агента: `Инженерный результат` и `Объясняющий материал`. Они задают разные роли и инструкции каждой стадии, но не управляют переходами; профиль фиксируется после начала задачи. Ошибка API или контракта ответа сохраняет ту же точку повтора, а токены фактически полученного невалидного ответа учитываются.
- Добавлены автодемо с остановкой после первого execution-шага, просмотр артефактов и фактического prompt, документация, безопасный шаблон окружения и корневая ссылка на день 13. Прошли 14 unit/headless-тестов, компиляция, CLI-справка, `uv lock --check` и `git diff --check`. Реальный DeepSeek API, интерактивная визуальная проверка и видео не выполнялись.

## 17.09.2026 11:28

- Все 18 каталогов локальных экспортов чата переименованы из неоднозначного формата `dd.MM` в сортируемый ISO-формат `YYYY-MM-DD`; год подтверждён датами сообщений внутри каждого `result.json`.
- В локальном `chats/README.md` обновлены ссылки на экспорты и явно зафиксирован формат имён каталогов. Проверены наличие всех 18 экспортов, соответствие каталога датам сообщений и отсутствие ссылок на прежние рабочие пути.

## 17.09.2026 11:19

- Корневые инструкции дополнены назначением проекта, ссылкой на недельную программу AI Advent Challenge 9 и явным правилом хранения всех заданий по каталогам `day-XX`.
- Закреплено, что упоминание чатов относится только к локальному каталогу `./chats/`, а все не предназначенные для Git материалы ДЗ должны находиться в `day-XX/resources/`.
- Из навыка публикации в общие правила вынесены устойчивые требования: однозначный выбор активного дня, разделение режимов публикации, проверка фактических Git/rclone-целей, доказательная фиксация результатов, защита посторонних изменений и обязательная проверка удалённых артефактов.

## 15.09.2026 22:02

- README дня 12 дополнен фактической ручной демонстрацией: восемь запросов в трёх сессиях показали три коротких пункта техлида, учебные разделы и код наставника, трёхстрочные ответы `Стихоплёта`, автоматическое сохранение формата на следующих вопросах и немедленное изменение ответа после переключения профиля.
- Видео `day-12-result.mp4` технически проверено: MP4/H.264, 2560×1440, 170,32 секунды, 6 625 782 байта, одна видеодорожка без аудио. Файл загружен через `rclone` в `AI Advent Challenge 9/day-12/`; удалённые размер и MD5 `f174fd72499f9ae4df20e9d61131d35a` совпали с локальными.
- Публичный API Яндекс Диска подтвердил имя, MIME `video/mp4`, размер и антивирусный статус `clean`; ссылка добавлена в README. Повторно прошли 18 тестов, компиляция, CLI-справка и проверка `uv.lock`; содержимое видео визуально не проверялось.

## 15.09.2026 21:59

- Добавлен repo-scoped навык `publish-homework` для трёх явных режимов: публикация только видео, публикация дня и полная публикация ДЗ. В корневом `AGENTS.md` закреплены фразы-триггеры и правило определения активного `day-XX`.
- Навык требует найти и технически проверить локальное видео, загрузить его точечно через `rclone`, подтвердить удалённый объект и публичную ссылку, а для публикации дня — проверить решение, обновить README с фактическими выводами, безопасно закоммитить и отправить только относящиеся к дню изменения.
- Полная публикация считается завершённой только после проверки видео на Яндекс Диске и коммита в удалённой ветке; итог обязан содержать ссылки на видео и README. Структура и frontmatter навыка успешно проверены штатным `quick_validate.py`.

## 15.09.2026 21:20

- Исправлено создание профиля в TUI дня 12: новая форма больше не содержит незаметно пустые обязательные поля и сразу получает валидные нейтральные значения. Профиль можно сохранить без заполнения каждого поля вручную.
- Сообщение об успешном сохранении или конкретной ошибке теперь отображается непосредственно рядом с действиями профиля. Добавлен headless-тест реального сценария кликов `Новый` → `Сохранить` с проверкой селектора и JSON-хранилища.

## 15.09.2026 21:06

- День 12 реализован как самостоятельный `uv`-проект без слоёв памяти: Textual-интерфейс позволяет создавать, редактировать, выбирать и безопасно удалять структурированные профили со стилем, форматом, языком, обращением и ограничениями.
- Активный профиль сохраняется отдельно для каждой сессии и добавляется в единый `system` каждого запроса; история не содержит копий профиля. Сессия атомарно хранит завершённые пары и последний фактически отправленный массив `messages`, а ошибки API не изменяют диалог.
- Добавлены два контрастных демонстрационных профиля и автодемо из трёх вызовов: одинаковый вопрос для техлида и наставника, затем вопрос без повторения настроек для проверки автоматической персонализации. Профили и сессии сохраняются в раздельных локальных JSON.
- Успешно прошли 17 тестов с `httpx.MockTransport` и headless Textual. Проверены CRUD профилей, привязка к сессиям, состав каждого prompt, восстановление истории, повреждённые данные и транзакционность. Внешний DeepSeek API и интерактивная визуальная проверка не выполнялись.

## 14.09.2026 18:11

- README дня 11 дополнен фактическим ручным прогоном на `deepseek-v4-flash`: восемь запросов показали общий ответ без памяти, зелёный из long-term, синий из working, английские названия из short-term, сброс формата после новой сессии и изоляцию working после новой задачи. Итоговый счётчик интерфейса составил 1 724 токена.
- Видео проверено технически и по двенадцати кадрам на протяжении записи: MP4/H.264, 2560×1440, 301,76 секунды, 3 394 016 байт, одна видеодорожка без аудио; видны все слои, переходы между scope, история и сохранённые prompts, секреты не обнаружены.
- `day-11-result.mp4` загружен через `rclone` в `AI Advent Challenge 9/day-11/`. Публичный API Яндекс Диска подтвердил MIME `video/mp4`, антивирусный статус `clean` и совпадающие локальные/удалённые MD5 `d9fd1d6249ab489e5287bb469ba4a5e3` и SHA-256 `37efb75084aeb01faa2829cf604538c2311c7426e2de607af1d09c483626c6ca`; публичная ссылка добавлена в README.

## 14.09.2026 17:53

- Формат short-term этапа автодемо упрощён: вместо HEX-кода модель временно называет цвет по-английски (`Blue`), а после новой сессии снова по-русски (`Синий`). Контрольный вопрос на всех этапах требует ответ из одного слова.
- Локальная игнорируемая памятка для видеозаписи синхронизирована с новым сценарием. Сохранённые пользовательские результаты не изменялись.

## 14.09.2026 17:33

- Последний фактически отправленный массив `messages` теперь сохраняется атомарно вместе с завершённым диалогом каждой сессии и восстанавливается во вкладке prompt при навигации по истории.
- Старый формат сессии в виде одного массива сообщений остаётся читаемым без миграции и перезаписи; для такой сессии интерфейс честно сообщает, что prompt ещё не сохранялся, и добавляет его после следующего успешного ответа.
- Регрессионные проверки подтверждают побайтно значимый состав prompt после повторного открытия `MemoryLayers` и после выбора старой сессии в headless TUI. Полный набор из 21 теста прошёл без внешних API-вызовов.

## 14.09.2026 17:25

- Автодемо дня 11 упрощено: вместо выбора архитектуры используется короткий сценарий цвета кнопки. Long-term хранит любимый зелёный цвет, working — назначение кнопки «Сохранить», short-term — временный язык названия цвета; контрольный вопрос требует ответить одним словом.
- Headless-тест автодемо теперь проверяет не только число вызовов, но и точное появление каждого слоя в нужном API-prompt и исчезновение short-term после новой сессии. Существующие локальные результаты прошлого сценария не изменялись.

## 14.09.2026 17:11

- В TUI дня 11 добавлен отдельный выбор сохранённой сессии. Выбор задачи больше не создаёт новую сессию, а загружает её последний диалог; выбор сессии восстанавливает сообщения в чате, и разговор можно продолжить.
- Short-term теперь физически разделена одновременно по задаче и сессии: `short_term/<task_id>/<session_id>.json`. Старые пользовательские файлы из `short_term/<session_id>.json` без изменения и переноса доступны как история `task-01`; правило Git-ignore расширено на JSON во всех вложенных каталогах `data/`.
- Автодемо сохраняет все шесть этапов в доступных для просмотра сессиях первой задачи, после чего переключается на новую задачу. Суммарные токены и последний фактический prompt не теряются при навигации; даже пустые новые задачи и сессии сохраняются в списках после перезапуска.
- Успешно прошёл 21 unit-тест с `httpx.MockTransport` и headless Textual, включая повторное открытие сессии, изоляцию одинаковых session ID разных задач и совместимость со старым форматом. Внешний API и интерактивная визуальная проверка не выполнялись.

## 14.09.2026 15:45

- День 11 оформлен как самостоятельный `uv`-проект с двухпанельным Textual-интерфейсом: слева единый чат и явные действия маршрутизации, справа одновременно видны short-term, working и long-term с их session/task/user scope, а на отдельной вкладке — последний фактический prompt DeepSeek.
- Три слоя физически разделены по JSON-файлам. Успешный обмен атомарно сохраняется только в текущую сессию, рабочие заметки — только в активную задачу, долговременные — в профиль пользователя; новая сессия сохраняет working/long-term, новая задача — только long-term. Ошибочные и незавершённые API-ответы историю не меняют.
- Добавлено автодемо из шести запросов: одинаковый вопрос сравнивается без памяти, с long-term, с working и со всеми слоями; затем новая сессия и задача наглядно показывают границы памяти. RAG, автоматическая классификация и state machine намеренно не добавлялись.
- Успешно прошли 16 unit-тестов с `httpx.MockTransport` и headless Textual, компиляция Python-файлов, `uv lock --check`, CLI-справка и `git diff --check`. Внешний DeepSeek API, интерактивная визуальная проверка и запись видео не выполнялись.

## 12.09.2026 20:49

- Видео дня 10 проверено технически и по выборочным кадрам: MP4/H.264, 2560×1440, 241,01 секунды, без аудиодорожки; видны пустой старт, работа всех стратегий и итоговая таблица, секреты не обнаружены.
- Файл загружен через `rclone` в `AI Advent Challenge 9/day-10/`. Публичный API Яндекс Диска подтвердил размер 12 822 606 байт, MIME `video/mp4`, антивирусный статус clean и совпадение локальных и удалённых MD5 `259c970b0c991dcb0d8d5cf5f6e4913d` и SHA-256 `ef40ca912edd5adcccb275f18b653a8485ee3982d1d7f3cf7e29e1eea5ca5ac0`; публичная ссылка добавлена в README.

## 12.09.2026 20:40

- README дня 10 сокращён до устройства трёх стратегий, запуска, фактического эксперимента, результатов и выводов; подробные контракты, обработка ошибок и лишние детали реализации удалены.
- Зафиксирован успешный пользовательский прогон на `deepseek-v4-flash` с `N=6`: 49/49 API-вызовов, Sliding 3/7 за 2 987 токенов, Facts 7/7 за 10 243 токена, Branching 7/7 по ручной оценке и 7 322 токена за обе ветки. Изоляция альтернатив прошла 4/4 в каждой ветке.
- Формальные 6/7 для веток объяснены ограничением строкового валидатора: ответ сохранил смысл интернет-требования, но сократил фразу. Добавлен вывод о цене Sliding, стабильности Facts и назначении Branching; исходный JSON-отчёт связан из README. Проверен `git diff --check`; код и внешний API повторно не запускались, визуальная проверка агентом и запись видео не выполнялись.

## 12.09.2026 20:19

- День 10 оформлен как самостоятельный `uv`-проект с четырьмя вкладками Textual: три независимые двухпанельные сессии Sliding Window, Sticky Facts и Branching плюс итоговое сравнение. Слева каждой стратегии находится чат, справа — состояние памяти, сообщения текущего контекста и последний фактически собранный prompt; общие модель и N блокируются после начала эксперимента.
- Sliding Window удаляет из API-контекста всё кроме последних N сообщений, Facts перед каждым рабочим ответом транзакционно обновляет отдельный JSON дополнительным учитываемым вызовом, а Branching хранит общий префикс и изолированные продолжения A/B от одного checkpoint. Для ветвления отдельно считаются общий префикс, стоимость каждой версии и полный двухветочный процесс.
- Автодемо использует одну спецификацию конференции: десять сообщений в каждой последовательности, ранние требования, заменённые решения, две несовместимые ветки и детерминированную проверку семи актуальных фактов и четырёх признаков отсутствия межветочного загрязнения. Полный синтетический прогон моделирует 49 API-вызовов и сохраняет безопасный атомарный JSON-отчёт.
- Успешно прошли 20 unit-тестов с `httpx.MockTransport` и headless Textual, включая точный состав prompt, учёт токенов невалидных и незавершённых ответов, транзакционность facts, изоляцию веток и независимость вкладок; также проверены `uv.lock`, компиляция, CLI-справка, `git diff --check` и отсутствие ключа в отслеживаемых файлах. Реальный DeepSeek API, интерактивная визуальная проверка и запись видео не выполнялись.

## 10.09.2026 20:18

- День 9 оформлен как самостоятельный `uv`-проект с двухпанельным Textual-интерфейсом: одинаковое сообщение параллельно отправляется одной модели DeepSeek с полной историей слева и отдельно хранимым recursive summary плюс последними N сообщениями справа. Реализованы выбор модели и N, счётчик до следующего summary, транзакционное удаление старого префикса, раздельные токены рабочих и служебных вызовов, безопасные ошибки и очистка с подтверждением. Системный промпт разрешает общие знания и генерацию кода, но не переносит старые ограничения формата на новые запросы.
- Встроенное демо выполняет 15 ходов, три summary и контроль пяти актуальных фактов. После полного или частичного запуска атомарно сохраняется безопасный JSON-отчёт вне `resources`; в нём нет API-ключа, но есть запросы, ответы, токены, summary, ошибки, состояние памяти и качество. Финальный автоматический прогон дал 5/5 в обеих панелях и уменьшил последний prompt с 1 185 до 823 токенов; из-за 2 218 токенов суммаризатора весь короткий прогон стоил на 10,1% больше.
- В расширенной видеосессии после 25 ходов и 50 сообщений полный режим израсходовал 39 060 токенов, summary-режим с пятью суммаризациями — 35 836; последний prompt сократился с 3 827 до 2 504 токенов, общий расход — на 8,3%. Видео проверено технически и по шести кадрам: MP4/H.264, 2560×1440, 206,47 секунды, без аудио; секреты не обнаружены.
- Видео `day-09-result.mp4` загружено в `AI Advent Challenge 9/day-09/` на Яндекс Диске. `rclone` подтвердил удалённый размер 12 588 543 байта и создал публичную ссылку; публичный API вернул MIME `video/mp4`, антивирусный статус clean и совпадающие локальные и удалённые MD5 `6248e28d0cf5a7dd185ace1441497607` и SHA-256 `c9bd1cf150dd66ce3751349a88cdb23bb34ce51341feeb00a2d8c84f521e893d`. Ссылка добавлена в README. Успешно прошли 24 теста с `httpx.MockTransport` и headless Textual, `uv lock --check`, компиляция и проверка CLI.

## 10.09.2026 16:44

- Видео `day-08-result.mp4` загружено в `AI Advent Challenge 9/day-08/` на Яндекс Диске и опубликовано для просмотра по ссылке. Публичная ссылка добавлена в README дня 8.
- Публичный API Яндекс Диска подтвердил имя, размер 13 251 122 байта, MIME `video/mp4` и антивирусный статус clean. Удалённые MD5 `dde43f3baeb2adde8afd85af63b65e3c` и SHA-256 `0c959a66a253ba0d1cba56b6b3b17b9820a4b2cb3207b48986a8b8d624746cfc` совпали с локальными.

## 10.09.2026 16:28

- README дня 8 сокращён до фактического устройства эксперимента, запуска, результатов, выводов и проверок. Удалены повторяющиеся определения метрик, диагностические варианты полного вывода, подробный контракт запроса и отдельный раздел источников.
- Документ синхронизирован с финальным прогоном `experiment-20260910T132229Z`: 43 192 токена, $0.129597, найденный контрольный факт и HTTP 400 при 17 392 токенах против окна 16 385. Добавлены выводы о росте prompt и цены, доле истории, повторной обработке входа, отсутствии кэша и поведении при переполнении.
- Видео `day-08-result.mp4` технически проверено: MP4/H.264, 2560×1440, 140,03 секунды, без аудиодорожки. Содержимое кадров не проверялось; код и внешний API на этой итерации не запускались.

## 10.09.2026 15:39

- В эксперимент дня 8 добавлена живая трассировка: перед каждым вызовом показываются роли и число сообщений, токены текущей реплики, истории и prompt, затем содержимое реплики; после вызова — полный ответ модели, фактические токены, цена и время либо ошибка OpenRouter.
- Режим `--prompt-display preview` показывает начало, окружение контрольного факта и конец больших синтетических блоков с точным числом пропущенных строк. `full` выводит весь фактически отправляемый массив `messages`, `none` оставляет только метрики и ответы; для видео выбран читаемый preview.
- README и unit-тесты обновлены: проверяются фрагменты длинного prompt, наличие контрольного факта и последовательность семи request-, шести response- и одного error-события. Внешний API не вызывался.

## 10.09.2026 14:57

- Финальный вывод дня 8 приведён к буквальным терминам задания: добавлена легенда «текущий запрос / вся история / prompt API / ответ модели», а колонки первой таблицы переименованы соответственно.
- После HTTP 400 теперь явно перечисляется, что именно сломалось: ответ и точный `usage` не получены, ошибочная реплика не сохранена, большой запрос требует сокращения реплики либо контекста. Убрано слишком сильное утверждение о полной невозможности продолжения: короткий запрос ещё может поместиться в оставшийся бюджет.
- README и проверки текстового представления обновлены. Внешний API не вызывался; итоговая проверка выполняется локально на сохранённом реальном результате.

## 10.09.2026 14:43

- В выводе дня 8 тестируемая модель теперь явно показывается перед API-вызовами, в заголовке таблицы контекста и в итоговой сводке рядом с фактическим провайдером. README и тест текстового представления обновлены; внешний API не вызывался.

## 10.09.2026 14:34

- Вывод эксперимента дня 8 расширен и разделён на две читаемые таблицы. Первая показывает состав контекста, долю истории, расхождение локального и API-счётчика, заполнение и остаток окна, время, finish reason и статус; вторая — токены вызова, кэш, цену относительно первого хода и накопленные вход, выход, общий расход и стоимость.
- После таблиц автоматически формируются шесть выводов: рост prompt и цены, доля истории, повторно обработанный ввод, соотношение входа и выхода, точность локального счётчика и величина переполнения. JSON-отчёт дополнен HTTP-статусом, кодом провайдера, источником цены и причиной завершения.
- README синхронизирован с новым выводом. Успешно прошли 9 unit-тестов, включая проверку обеих таблиц и аналитических выводов; внешние API повторно не вызывались, визуальная проверка выполнена в широком терминальном представлении, видео не записывалось.

## 10.09.2026 11:30

- День 8 оформлен как самостоятельный `uv`-проект с одним измеряемым агентом и воспроизводимым семиходовым экспериментом. Для каждого хода показываются локальные токены новой реплики и истории, локальный и фактический prompt, ответ, кэш, цена и накопленный расход; OpenRouter context compression и fallback явно отключены.
- После проверки живого каталога недоступная `openai/gpt-3.5-turbo-0613` заменена на активную `openai/gpt-3.5-turbo-16k` с настоящим окном 16 385 токенов. Два предварительных запроса завершились 404 до генерации и без `usage`; успешный прогон выполнил шесть ответов OpenAI, нашёл контрольный факт в длинной истории и получил ожидаемый HTTP 400 на prompt из 17 392 токенов без изменения истории.
- В реальном прогоне prompt вырос со 127 до 14 555 токенов, суммарно обработано 43 192 токена за $0.129597, кэш не использовался; локальная оценка совпала с API на всех успешных ходах. Результаты и выводы добавлены в README, локальный JSON сохранён в игнорируемых `resources/results/`.
- Успешно прошли 9 тестов с `httpx.MockTransport`, `uv lock --check`, компиляция, локальный preview и `git diff --check`. Табличный CLI проверен в терминале; браузерная проверка и запись видео не выполнялись.

## 09.09.2026 09:53

- README дня 7 сокращён до описания готового решения, архитектуры, запуска, результа и проверок. Формулировка задания и покадровая инструкция по сдаче удалены.
- Видео `day-07-result.mp4` технически проверено: MP4/H.264, 2560×1440, 78,55 секунды, без аудиодорожки. Файл загружен в `AI Advent Challenge 9/day-07/` на Яндекс Диске; удалённые размер 1 099 288 байт и MD5 `a45b1e7f016ceaef240b5ecf97ef1ddc` совпали с локальными. Публичная ссылка добавлена в README; содержимое кадров повторно не проверялось.

## 09.09.2026 00:11

- День 7 оформлен как самостоятельный `uv`-проект на основе двухпанельной архитектуры дня 6. Сессии `left` и `right` загружают, показывают и передают модели всю свою переписку из отдельных `data/left.json` и `data/right.json`; метрики прошлых запусков не восстанавливаются.
- Добавлено JSON-хранилище с проверкой завершённых пар `user`/`assistant`, `fsync` и атомарной заменой файла. Ошибочные, незавершённые и несохранённые запросы не меняют историю; повреждённый JSON не перезаписывается молча. В каждой панели есть двухшаговая очистка, не затрагивающая соседнюю сессию.
- Добавлены безопасный `.env.example`, Git-ignore локальных JSON, описание архитектуры, запуска и покадровый сценарий записи. Прошли 14 локальных тестов с `MockTransport` и headless Textual, проверка `uv.lock`, компиляция, CLI-справка и `git diff --check`. Реальный DeepSeek API не вызывался; визуальная проверка и запись видео не выполнялись.

## 07.09.2026 22:32

- Запись результата дня 6 проверена технически и по выборочным кадрам: MP4/H.264, 2560×1440, 50,48 секунды, аудиодорожки нет; видны две независимые сессии, их ответы и отдельная статистика, секреты в кадре не обнаружены.
- Видео `day-06-result.mp4` загружено в `AI Advent Challenge 9/day-06/` на Яндекс Диске. Размер удалённого файла совпал с локальным — 583 534 байта; публичная страница с именем файла и видеопроигрывателем открывается без авторизации.
- Публичная ссылка на демонстрацию добавлена в README дня 6. Код не менялся; внешний DeepSeek API и повторные unit-тесты на этой итерации не запускались.

## 07.09.2026 22:12

- День 6 оформлен как самостоятельный `uv`-проект: в одном Textual-интерфейсе созданы две панели с отдельными экземплярами `Agent`, независимыми историями и состояниями выполнения; конфигурация и асинхронный HTTP-клиент переиспользуются.
- Агент инкапсулирует системный промпт, параметры DeepSeek, сборку полного стека сообщений, разбор ответа и статистику. Каждая панель показывает токены последнего вызова и всей сессии, число сообщений и запросов, время и `finish_reason`; ошибочный запрос в историю не записывается, автоматических повторов нет.
- Добавлены безопасный `.env.example`, инструкция запуска и сценарий демонстрации «Влад» только в левой сессии; корневое оглавление дополнено днём 6.
- Успешно прошли 6 локальных тестов с `httpx.MockTransport`, включая изоляцию контекста и токенов, ошибки API, создание двух панелей и отправку сообщений из обеих панелей; выполнены компиляция и проверка CLI-справки. Реальный DeepSeek API не вызывался, визуальная проверка TUI не выполнялась.

## 04.09.2026 22:16

- В README дня 5 добавлен фактический результат сессии `20260904T190746-98978d93`: таблица семи формализованных проверок, времени, токенов и оценочной стоимости трёх моделей.
- Зафиксирована ручная проверка полных ответов и короткий вывод: победитель прогона — DeepSeek V4 Flash, затем DeepSeek V4 Pro и Nemotron; отдельно отмечены ошибки каждой модели и нарушение Flash лимита 350 слов.
- Сохранённые проверки независимо пересчитаны из JSON и полностью совпали; все шесть ответов имеют `finish_reason=stop`. Изменения документации проверены через `git diff --check`; API повторно не вызывались, визуальная проверка не выполнялась.

## 04.09.2026 22:04

- Очищены лишние локальные материалы дня 5: старое видео, диагностические ответы Groq/OpenRouter, пустые каталоги результатов и проверок, корневой черновик `test.py` и сгенерированные `__pycache__` во всём репозитории.
- Сохранены `.env`, проектное `.venv`, исходное задание, тесты и regression fixture, поскольку они нужны для запуска и воспроизводимой проверки текущего решения.
- После очистки повторно выполнены 30 unit-тестов и `git diff --check`; реальные API-вызовы и визуальная проверка не выполнялись.

## 04.09.2026 22:01

- День 5 приведён к требованиям задания после повторной сверки: первый промпт получил однозначный JSON-раздел `Итог`, а локальная проверка независимо перебирает все 64 допустимых набора и проверяет три требуемых результата задачи релиза.
- Оба штатных промпта теперь имеют независимые проверяющие модули, привязанные к SHA-256 точного текста; автоматические баллы отделены от ручной оценки объяснения, полноты и итогового качества модели.
- `README.md` переписан по текущей реализации: зафиксированы шесть последовательных запросов без запроса к модели-анализатору, актуальный сценарий записи, метрики, ограничения проверок и отсутствие победителя до нового прогона; устаревшие результаты удалены.
- Локально прошли 30 unit-тестов, `uv lock --check`, проверка CLI и `git diff --check`; реальные API, визуальная проверка и новая видеозапись не выполнялись.

## 04.09.2026 21:52

- День 5 переведён на ручную оценку результатов пользователем и Codex. Седьмой запрос с ответами на анализ DeepSeek Pro удалён: новый прогон выполняет ровно шесть последовательных API-вызовов, а Pro остаётся только одним из трёх участников. Новые JSON не содержат analysis и blind_mapping; после ответов выполняются только локальные проверки.
- Интерфейс больше не показывает вкладку и метрики анализатора. В каждой задаче добавлена первая вкладка «Сравнение» с тремя карточками: фактическое имя модели, статус, время, токены, стоимость, локальный результат и первые 900 символов ответа. Полные ответы доступны в соседних вкладках моделей; после завершения автоматически открывается сравнение первой задачи. Обновлены тексты состояний, справка CLI, README, design.md и описание проекта.
- По дополнительному запросу удалена последняя сессия day-05/resources/results/20260904T183116-e18f8911.json. Каталоги resources/results и resources/verification пусты; диагностика API, видео, материалы задания и тестовые fixtures сохранены.
- Прошли 28 тестов с MockTransport/headless Textual: ровно шесть запросов, отсутствие седьмого вызова и полей анализа, последовательность, карточки и сокращение длинных ответов, локальные проверки, ошибки и открытие без сети. CLI-справка и git diff --check проверены. Реальные API-вызовы и визуальная проверка не выполнялись.

## 04.09.2026 19:42

- По просьбе пользователя общий лимит ответов участников дня 5 увеличен с 1800 до 4096 токенов. Причина: в последнем прогоне 20260904T163415-59039eff ответ Nemotron на первую задачу обрезан ровно на 1800 токенах с finish_reason=length. Лимит анализатора остаётся 3600; UI и новые JSON получают лимит из общей константы.
- Удалены оставшаяся сессия day-05/resources/results/20260904T163415-59039eff.json и отчёт day-05/resources/verification/20260904T160959-1773fc70.md. Оба каталога пусты. Диагностика подключений, видео, материалы задания и тестовые примеры сохранены. Обновлены README и design.md, убрана ссылка на ранее удалённый локальный JSON первого прогона.
- Прошли 30 локальных тестов с MockTransport/headless Textual, включая проверку лимита 4096 у всех трёх моделей и 3600 у анализатора; git diff --check пройден. Реальные API-вызовы и визуальная проверка не выполнялись.

## 04.09.2026 19:32

- Исправлен недостоверный вывод дня 5: стандартная задача Python проверяется независимо от Pro по исходному stdout, контрольным случаям контракта build, новому stdout и assert автора. Проверяющий модуль привязан к SHA-256 задачи; изменённые промпты, неоднозначный формат и неподдерживаемый код получают «Не проверено», без числового ранжирования. Четыре пункта имеют равный вес; полнота объяснений и тестов этим не доказывается.
- Добавлены verification.py и verification_worker.py: ограниченное AST-подмножество Python, отдельный процесс без окружения приложения, временный каталог, ограничения CPU/памяти/записи и timeout. Проверки нового прогона выполняются вне UI-потока до анализа; поток явно завершается, ожидание работает и при недоступности пробуждений asyncio из потока в sandbox.
- «Вывод» показывает локальные факты, таблицу и конкретные ошибки; «Мнение Pro» сохраняет исходный анализ с предупреждением. Общий победитель не подтверждается, поскольку проверки не покрывают обе задачи. Анализатор получает анонимные результаты локальных проверок. При просмотре сохранённой сессии проверки пересчитываются в памяти без сети и перезаписи JSON.
- Сессия 20260904T160959-1773fc70 перепроверена: Nemotron 2/4 (assert 5/7), Flash 3/4 (7/7), Pro 3/4 (8/8), все три функции проходят контрольные случаи. У Nemotron и Flash неверен исходный stdout, у Pro — новый stdout. Отдельный отчёт: day-05/resources/verification/20260904T160959-1773fc70.md; исходный файл побайтно сохранён. Для регрессии добавлены только текст задачи и ответы в fixtures/python-build-review.json, без API-метаданных.
- Прошли 30 тестов, включая MockTransport, headless Textual, неверное мнение анализатора, ничью, анонимизацию проверок, загрузку без сети, пересчёт подменённых оценок и недопустимый код. Отдельно подтверждено реальное ограничение CPU на длительном цикле. Тест совместимости v1 использует синтетическую запись и больше не зависит от отсутствующего пользовательского файла первого прогона; его удаление не отменялось. README и design.md обновлены. Реальные API-вызовы и визуальная проверка не выполнялись.

## 04.09.2026 19:08

- День 5 переведён с Groq на OpenRouter: OPENROUTER_API_KEY, конкретная бесплатная модель :free, reasoning.enabled=false, проверка поддержки параметров и ограничение цены входа/выхода нулём. Ключи не выводились; в локальном .env изменена только OPENROUTER_MODEL_WEAK, старые переменные Groq сохранены, но больше не используются новыми запусками.
- Qwen3 4B Free оказалась недоступна: публичный API вернул пустой список эндпоинтов, генерация — 404. Gemma 4 26B Free дала 429 общего пула Google AI Studio, подтверждённый limit_source=upstream_provider_shared_pool. Выбрана и прописана nvidia/nemotron-3.5-lightning:free: 30B общих/3B активных параметров, бесплатный эндпоинт NVIDIA.
- Оба исходных промпта подряд прошли реальную проверку Nemotron при max_tokens=1800: stop, 439/670 токенов за 14,77 с и 391/970 токенов за 39,08 с; reasoning пуст, usage.cost=0 у обоих. Результат отдельно в day-05/resources/diagnostics/openrouter-check-20260904T160416.json. Всего пять диагностических запросов генерации (Qwen 404, два Gemma 429, два успешных Nemotron), без DeepSeek и анализатора; полный сравнительный прогон не запускался.
- Общий лимит участников восстановлен до 1800, пауза Groq убрана. Новый анализ Pro вызывается только после шести полных ответов; при ошибке или обрезании показывается неполное сравнение без победителя. В старых неполных сессиях сохранённый анализ помечается недостоверным для определения общего победителя; исходные JSON не переписываются. Названия вкладок и сводки теперь берутся из фактических моделей сессии.
- Обновлены .env.example, README и design.md. Прошли 23 теста с MockTransport/headless Textual, компиляция, CLI-справка, git diff --check; проверены новые ключи/эндпоинт/бесплатная маршрутизация, очередь, исторические записи и запрет анализа неполного прогона. Визуальная проверка не выполнялась.

## 04.09.2026 18:46

- Разобрана повторная ошибка Groq в последовательной сессии `20260904T153925-e094f7e2`: между запросами было 21,06 с, второй получил 429 при оставшихся 7304/8000 TPM и 999/1000 RPD без retry-after. Отдельный диагностический вызов подтвердил `Request too large` по выходным токенам: OTPM Limit 1000, Requested 1237 при полностью свободном TPM. Причина не устраняется фиксированной паузой.
- Общий лимит ответа трёх участников снижен с 1800 до 900 токенов для соблюдения ограничения Groq и одинаковых условий сравнения; лимит анализатора 3600 сохранён. Лимит виден в интерфейсе и JSON. Добавлено распознавание ошибки превышения OTPM размером запроса с безопасным сохранением только вида лимита и чисел.
- Контрольный запрос только второй задачи к Groq Free с max_tokens=900 успешен: stop, 382 входных/607 выходных токенов, 1,87 с. Результат сохранён отдельно в игнорируемом `day-05/resources/diagnostics/groq-output-limit-check.json`. За итерацию выполнены два диагностических вызова Groq (ошибка и проверка исправления); DeepSeek и анализатор не вызывались, исходные эксперименты не изменялись.
- Прошёл 21 локальный тест с MockTransport/headless Textual, включая общий лимит, обработку OTPM и исключение секретов/идентификаторов из ошибки; git diff --check пройден. README и design.md обновлены с причиной, проверкой и отличием нового лимита от прошлых условий. Полный эксперимент с лимитом 900 и визуальная проверка не выполнялись.

## 04.09.2026 18:38

- В день 5 добавлена асинхронная пауза 2 секунды перед каждым запросом к Groq, по просьбе пользователя. Статус «Пауза 2 с» отделён от активного запроса; время паузы не входит в длительность HTTP-вызова и сохраняется отдельной метрикой. Для DeepSeek порядок и задержки не менялись.
- Прошли 20 тестов с подменой API и ожидания, включая проверку двух пауз перед Groq, сохранения статуса до отправки и последовательного порядка запросов. `git diff --check` пройден. Реальные API-вызовы и визуальная проверка не выполнялись.

## 04.09.2026 18:34

- В сессии дня 5 `20260904T152657-d9e57723` подтверждён HTTP 429 у Qwen/Groq по второй задаче: первый запрос успешен за 3,21 с, второй отклонён через 0,35 с; старты отличаются примерно на 10 мс. Конкретный лимит и причинная роль одновременного запуска не установлены, поскольку старая запись не содержит заголовков квоты. Проверена официальная документация Groq; README дополнен разбором.
- По просьбе пользователя шесть запросов переведены на последовательное выполнение: Qwen → Flash → Pro по первой задаче, затем по второй, после них анализ Pro. Добавлены сохранение режима/текущего запроса, статусы очереди и таймер только активного запроса. Время ожидания очереди не входит в длительность ответа; автоповторов нет. Для будущих HTTP 429 добавлены понятное сообщение и сохранение только числовых заголовков лимитов/retry-after.
- В выводе анализатора анонимные обозначения заменяются фактическими названиями моделей по сохранённому соответствию. Для новых запросов используются MODEL_A/MODEL_B/MODEL_C; старые A/B/C заменяются в контекстах участников с сохранением кода и обозначений задач релиза. Исходный JSON не переписывается, анализатор названий моделей в метаданных не получает.
- Прошли 20 тестов: порядок запросов без перекрытия, очередь в headless Textual, анонимизация, подстановка названий, защита наборов задач/кода, обработка HTTP 429 и исключение произвольных заголовков/секретов. Подстановка отдельно проверена на последней сохранённой сессии с сохранением исходного файла. `git diff --check` пройден. Реальные API-вызовы и визуальная проверка не выполнялись.

## 04.09.2026 18:25

- Увеличена высота редакторов обоих промптов дня 5 с 8 до 12 строк, контейнера с вкладками — с 12 до 16 строк, чтобы при редактировании было видно больше текста.
- Проверены сохранённые CSS-значения и `git diff --check`. Визуальная проверка не выполнялась.

## 04.09.2026 18:20

- День 5 расширен до двух разных редактируемых промптов на трёх моделях: шесть независимых запросов стартуют параллельно, седьмой запрос к DeepSeek Pro анализирует обе задачи после завершения всех ответов. Добавлена задача разбора Python-кода с замыканиями и изменяемым аргументом по умолчанию для сравнения на другом типе задания.
- Добавлены таблица шести ответов, вкладка сводных токенов/суммы длительностей/стоимости по каждой модели и просмотр полных ответов по задаче и модели. Неизвестная метрика даёт неизвестный итог; расходы анализа отделены. Пустые и одинаковые промпты блокируются до API-вызовов.
- Анализатор получает постоянное соответствие A/B/C в обеих задачах без названий моделей/провайдеров в метаданных. Инструкция требует оценки каждой задачи, средние оценки с равными весами и явный общий результат по правильности, полноте и соблюдению условий; возможны ничья и неполное сравнение. Лимит анализа увеличен до 3600 токенов; режимы исследуемых моделей не менялись.
- Новый JSON v2 группирует результаты по задаче и модели и хранит числовую сводку. Старые записи v1 открываются через преобразование в памяти без перезаписи. README и design.md обновлены; первый записанный эксперимент явно отделён от ещё не выполненного нового.
- Прошли 18 тестов с MockTransport и headless Textual, включая одновременный старт шести запросов, порядок анализа, анонимизацию, частичные ошибки, неизвестные метрики, редактирование двух задач, переключение всех вкладок ответов и чтение старой сессии без сети. Проверены компиляция, CLI-справка, git diff --check; исходный stdout Python-промпта и исправленная реализация его контракта проверены локально. Визуальная проверка и реальные API-вызовы не выполнялись; запуск для записи оставлен пользователю.

## 04.09.2026 18:00

- Проверено ДЗ дня 5 по коду, исходной сессии `20260904T144124-d6613a85` и видео пользователя. Все четыре API-вызова завершились успешно; входные сообщения одинаковы, три модели стартовали в пределах 40 мс, анализ начался после всех ответов. Расход: 3948 токенов на ответы и 4587 на анализ, оценка общей стоимости $0,00598642.
- README дополнен фактическими метриками и выводом на основе полного перебора 64 наборов. Отмечены ошибки Qwen при бюджете 12, неверная вторая альтернатива Pro, нестрогие рассуждения Flash и противоречие в итоговом LLM-анализе. Исходный JSON в `day-05/results/` и видео сохранены без изменений; разъяснено отличие опубликованной сессии от рабочих записей в `resources/results/`.
- Исправлен тариф кешированного ввода DeepSeek Pro с $0,021 на $0,022 за миллион токенов off-peak ($0,044 peak), добавлен регрессионный тест. На стоимость записанной сессии это не влияет: кешированных токенов Pro было 0.
- Прошли 16 тестов с подменой API, `uv sync --locked --offline`, компиляция, CLI-справка и `git diff --check`. Метрики и ответы сверены с исходными данными; проверены Git-ignore, отсутствие секретов в публикуемых файлах и сохранение SHA-256 исходных артефактов. Видео MP4/H.264, 1656×1124, 166,786 секунды, без аудио; просмотрены выборочные кадры запуска, анализа и ответов. Декодирование с сохранением временной базы прошло без ошибок; предварительные сообщения null-muxer о DTS устранены настройкой проверки, исходник не перекодировался. Новых LLM-вызовов не было.
- По прямому запросу пользователя материалы подготовлены к коммиту и отправке в `GaliFrey/ai-advent-challenge-9`, ветка `main`; видео — к загрузке через настроенный rclone `yadisk` в `AI Advent Challenge 9/day-05/day-05-result.mp4` с получением публичной ссылки. Ключи, чаты и видео исключены из Git.

## 04.09.2026 17:31

- День 5 реализован как самостоятельный `uv`-проект с Textual, httpx и python-dotenv. Одна кнопка запускает общий запрос к Qwen и двум DeepSeek параллельно; после завершения всех трёх запросов автоматически вызывается DeepSeek Pro для анализа полных ответов и метрик под обозначениями A/B/C. Анализ показывается в том же терминальном окне, его расходы отделены от участников.
- Добавлены редактируемая начальная задача планирования релиза, таблица статусов/времени/токенов/стоимости, вкладки полных ответов, обработка ошибок и обрезанных ответов без автоповторов. Сессии сохраняются после каждого результата в локальном `day-05/resources/results/`; повторный просмотр не вызывает API. Заполненный `.env` не изменялся; добавлен безопасный `.env.example`.
- В README и design.md описаны сценарий видео, управление, тарифные допущения и ограничения LLM-оценки. Роли моделей обозначены условными: размер Qwen не доказывает её слабость. Обновлено корневое оглавление. OpenSpec не устанавливался.
- Прошли 15 локальных тестов с `httpx.MockTransport` и Textual `run_test`, включая параллельность, порядок четвёртого вызова, сохранение, обработку сбоев, отсутствие ключей в артефактах, защиту от повторного нажатия и просмотр без сети. Проверены `uv sync --locked --offline`, CLI-справка, компиляция, Git-ignore и `git diff --check`. Геометрия блоков после сворачивания задачи проверена в headless-окне 110×40; визуальная проверка в пользовательском терминале, браузер и реальные API-запуски не выполнялись. Запуск для видео оставлен пользователю.

## 04.09.2026 17:09

- В локальный `day-05/.env` добавлены согласованные модели: `DEEPSEEK_MODEL_MEDIUM=deepseek-v4-flash`, `DEEPSEEK_MODEL_STRONG=deepseek-v4-pro` и `GROQ_MODEL_WEAK=qwen/qwen3.8-27b`. Строки дописаны без изменения существующих ключей.
- Проверены сохранённые настройки моделей и исключение `.env` из Git; `git diff --check` пройден. API-вызовы не выполнялись, чтение этих настроек кодом ещё не реализовано.

## 04.09.2026 17:08

- По запросу пользователя создан локальный `day-05/.env` с пустыми переменными `DEEPSEEK_API_KEY` и `GROQ_API_KEY` для самостоятельного заполнения ключей двух провайдеров.
- Проверены наличие переменных и исключение файла из Git. Реализация и внешние API-вызовы не выполнялись.

## 04.09.2026 16:03

- Изучены задание дня 5 и уточнения в чате: сравнение через API, выбор трёх моделей разного уровня, возможность повторить прежнюю логическую задачу и необязательность ссылок на модели. Реализация дня 5 не начиналась.
- По запросу пользователя очищен экспорт `chats/04.09/result.json`: из 91 записи сохранены 74, удалены 17 записей с флудом, повторами, служебным событием и посторонней новостью. Удалена одна фотография к новости, сохранены 10 скриншотов с результатами и техническим контекстом. У одной сохранённой реплики убрана ссылка на удалённое сообщение; остальное содержимое не менялось. Обновлено оглавление `chats/README.md`.
- Исходный экспорт, документация до очистки и журнал отбора с SHA-256 сохранены в `/tmp/ai-advent-chats-0409-backup-20260904-s1yuzci8/`. Проверены резервная копия, валидность JSON, порядок и неизменность сохранённых сообщений, связи и хеши вложений, неизменность предыдущих экспортов. Скриншоты просмотрены для отбора; внешние API и браузер не запускались, сведения участников о моделях и тарифах по внешним источникам не проверялись.

## 04.09.2026 10:46

- Очищен новый экспорт `chats/03.09/result.json`: из 170 записей оставлены 82 полезных сообщения, удалены 88 записей с флудом, повторами и служебными событиями. Сохранены задание дня 4, технические обсуждения, результаты экспериментов, ссылки и итоговые ответы организатора по правилам сдачи.
- Удалены две фотографии к сообщениям о сбоях сервисов; сохранены 13 технических вложений. В четырёх сообщениях удалены ссылки на удалённые реплики, остальное содержимое сохранённых сообщений не менялось. Обновлено оглавление `chats/README.md`.
- Полный исходный экспорт и журнал отбора с SHA-256 сохранены в `/tmp/ai-advent-chats-0309-backup-20260904-6503sxag/`. Проверены резервная копия, валидность JSON, неизменность текстов и порядка, связи между сообщениями и вложения; подтверждено, что экспорты предыдущих дней не изменились. Скриншоты просмотрены для отбора; внешние API и браузер не запускались, утверждения участников отдельно не проверялись.

## 03.09.2026 16:11

- README дня 4 сокращён до задания, описания эксперимента, команд запуска, результатов и вывода: удалены история переделок, повторяющиеся пояснения и устаревшие указания о будущей записи.
- Добавлены метрики записанной серии `v3`: 18 ответов без ошибок API, 8451 токен, верные фактические ответы при всех температурах и 1/2/3 уникальных слогана. По двум тезисам пользователя написан вывод от первого лица: на точной задаче температура почти не повлияла на результат, а высокая температура в творческой задаче иногда приводила к бессвязному тексту.
- Метрики сверены с исходными ответами с повторной локальной валидацией, проверены ссылки README и состав материалов для Git. Код и ответы не изменялись, новых API-вызовов и визуальной проверки видео не было. Код, результаты и документация подготовлены к ранее согласованной публикации в `origin/main`; `.env` и видео остаются локальными.

## 03.09.2026 15:55

- В дне 4 творческий промпт `v3` полностью отделён от фактического: остались слово «Орбита», слоган и три свободные идеи; данные выставки, музей, билеты, расчёты и блок `facts` исключены из запроса. Контрольный промпт `facts` сохранён без изменений, чтобы отдельно сравнить температуры на точной и творческой задачах.
- Валидатор, CLI и отчёты учитывают разные схемы ответов: у творчества проверяются JSON, два поля и завершение, фактическая точность не оценивается. Из творческой консольной таблицы убран столбец фактов, в Markdown вместо него прочерк. Чтение старых форматов `v1`/`v2` продолжает проверять их фактический блок; смешивание версий в одной сессии запрещено.
- По запросу пользователя удалены все 22 файла прежней серии из `day-04/results/`: 18 ответов, манифест и три отчёта. README подготовлен к новой сессии `video` с прежними шестью командами; результатов и выводов новой серии пока нет.
- Прошли 35 локальных unit-тестов на синтетических данных, включая отсутствие музейного контекста и оценки фактов у творчества, совместимость старых ответов и накопление результатов шести запусков. Текстовая ширина сводки проверена тестом; визуальная проверка терминала и реальные API-вызовы не выполнялись. Запуски для видео оставлены пользователю, публикация отложена до его выводов в README.

## 03.09.2026 15:34

- Творческий промпт дня 4 смягчён: «Орбита» стала отправной точкой для свободных идей с произвольными жанром, аудиторией, местом действия и степенью фантастичности. Привязка к музейным активностям и реальной осуществимости снята; контрольный запрос `facts` и проверяемые расчёты не менялись.
- Добавлена версия промптов `v2` в манифесты, сохранены исходные тексты `v1` для совместимости формата. Проверка серии учитывает её версию, а смешивание старых и новых промптов при записи блокируется до API-вызова.
- По прямому запросу пользователя удалены все 23 файла старой сессии `day-04/results/video/`: ответы, манифест, сводки и наблюдения. Раздел результатов в README очищен; шесть обычных команд подготовлены для новой чистой сессии `video` с промптами `v2`.
- Прошёл 31 локальный unit-тест, компиляция, проверка команд и документации; подтверждено, что каталог результатов пуст. Реальные API-вызовы и публикация не выполнялись, запуск оставлен пользователю для записи. Новое поведение модели и визуальный вывод на его терминале не проверялись.

## 03.09.2026 15:25

- `compare --compact` дня 4 теперь выводит две выровненные таблицы для терминала вместо сырого Markdown, затем фактические слоганы по температурам. Метрики вынесены в общий расчёт для Markdown и консоли; текстовая версия сохраняется в `summary.txt`.
- Разобрана видеосессия пользователя: 18/18 ответов прошли автоматическую проверку, расход 11 464 токена. При T=0 и T=0.7 слоган одинаков во всех шести ответах, при T=1.2 — три разных; квест встречается во всех девяти творческих ответах. Сходство тем и ограничения постановки описаны в `results/video/observations.md` без рейтинга креативности.
- README приведён к результатам видеосессии; убраны устаревшие ссылки и таблица предварительной серии, файлы которой уже отсутствуют в каталоге результатов. Выводы пользователя и публикация остаются отложенными. Промпты и исходные ответы не менялись.
- Прошли 28 локальных тестов и `git diff --check`. Сводка пересчитана без API; SHA-256 исходных JSON и числовые Markdown-метрики сохранились. Текстовый вывод текущей сводки проверен на ширину до 78 символов. Изучен присланный скриншот; новый вывод в терминале пользователя и видеозапись визуально не проверялись. Внешних API-вызовов и публикации не было.

## 03.09.2026 15:15

- В режиме записи дня 4 перед первым API-вызовом один раз выводятся фактическая системная инструкция и полный выбранный промпт; перед тремя ответами они не повторяются.
- JSON-ответы представлены в консоли читаемыми строками: русские названия полей фактов, отдельный слоган и нумерованные идеи. Значения и дополнительные поля сохраняются, исходный JSON остаётся в результатах без изменений. Формат запроса к модели и автоматический валидатор не менялись.
- README поясняет назначение JSON и новый вид вывода. Прошли 28 локальных тестов, включая проверку однократного вывода промпта до первого вызова и сохранения всех значений; проверен текстовый пример на синтетических данных и `git diff --check`. Новых API-вызовов, браузерной/визуальной проверки и публикации не было.

## 03.09.2026 14:52

- Для записи видео добавлена команда дня 4 `record --prompt facts|creative --temperature 0|0.7|1.2`: каждый запуск выполняет ровно три независимых вызова и показывает все ответы компактно, с входными/выходными/суммарными токенами, временем и автоматической проверкой. Текст промпта выбирается параметром и не дублируется в консоли.
- Шесть запусков дополняют общую сессию `results/video/`; после каждого ответа сохраняется исходный JSON, после команды обновляются полные ответы и сводка. Повтор уже начатого сочетания запрещён до API-вызова; `--session` позволяет создать отдельную запись без перезаписи прошлых результатов.
- Добавлен `compare --compact` для итоговой таблицы на видео. README содержит шесть отдельных команд и явно отделяет предварительную серию от будущей видеосессии; выводы пользователя и публикация остаются отложенными.
- Прошли 28 unit-тестов, компиляция и CLI-справка; проверены накопление всех 18 ответов на синтетических данных, сохранение сбоев, защита от повторного вызова и совместимость чтения старой серии. Новых внешних API-вызовов, браузерных/визуальных проверок и публикации не было.

## 03.09.2026 14:38

- День 4 реализован как самостоятельный `uv`-проект: два промпта на общих исходных данных (факты/расчёты и факты/творчество), температуры 0, 0.7 и 1.2, по три независимых повтора. Thinking mode выключен, остальные настройки зафиксированы.
- Выполнены 18 реальных вызовов DeepSeek: фактическая задача прошла автоматическую проверку 9/9, творческая — 8/9. При T=1.2 сохранён ответ с ошибочной стоимостью 1480/1258 вместо 1960/1666; повторных запросов ради исправления не было. Расход — 11 424 токена.
- По прямому запросу пользователя все промпты, настройки, полные ответы API, метрики, сводная таблица и наблюдения размещены в доступном для Git `day-04/results/20260903T113436017263Z/`. Сводка заново проверяет исходные ответы; текстовая уникальность отделена от смысловой оценки творчества.
- README дня содержит фактические результаты и место для выводов пользователя. Обновлено корневое оглавление. Коммит и публикация отложены до внесения этих выводов; `.env`, видео и вложения курса остаются локальными.
- Прошли 19 локальных unit-тестов, `uv sync --locked --offline`, компиляция, CLI-справка и пересчёт сохранённой серии. Проверены JSON-артефакты, Git-ignore и отсутствие пробельных ошибок. Браузерная/визуальная проверка и запись видео не выполнялись.

## 03.09.2026 14:21

- По запросу пользователя очищены четыре дневных экспорта в локальном `chats/`: из 1106 записей сохранены 579 полезных сообщений, удалены 527 записей с флудом, повторами и служебными событиями.
- Сохранены задания, уточнения требований, технические вопросы и ответы, ссылки и результаты экспериментов; удалены 20 фотографий к удалённым сообщениям, оставлены 26 полезных вложений. В 14 сообщениях убраны ссылки на удалённые реплики; остальное содержимое сохранённых сообщений не изменено.
- Добавлено локальное оглавление `chats/README.md` со статистикой и критериями отбора. Полные исходники и журнал очистки сохранены вне репозитория в `/tmp/ai-advent-chats-backup-20260903-YuhPIQ/`; `chats/` остаётся исключённым из Git.
- Проверены SHA-256 всех исходных файлов и резервных копий, валидность JSON, порядок и неизменность сохранённых сообщений, наличие заданий и целостность связей и локальных вложений. Спорные скриншоты просмотрены для отбора; приложения и внешние API не запускались, технические утверждения участников отдельно не проверялись.

## 02.09.2026 17:24

- Выполнен и записан итоговый эксперимент дня 3: все четыре способа получили `PASS`, суммарный расход составил 105, 383, 800 и 663 токена; отдельный LLM-анализ использовал 2962 токена.
- README синхронизирован с финальным прогоном и содержит вывод о равной точности одного запуска при различной эффективности способов.
- Видео проверено технически и по выборочным кадрам: MP4/H.264, 1636×1216, 134,024 секунды; видны четыре запуска и итоговое сравнение, секреты не показаны. Аудиодорожка отсутствует.
- Повторно успешно выполнены 22 unit-теста, компиляция Python-файлов, проверка `uv.lock`, локальное сравнение и `git diff --check`; внешние API-вызовы при проверке не выполнялись.

## 02.09.2026 17:10

- Перед новой итоговой записью из README дня 3 удалены устаревшие результаты предыдущих запусков; вместо них зафиксированы правила честной интерпретации нового эксперимента и различие математической корректности с соблюдением формата ответа.
- Валидатор теперь требует, чтобы единственная строка `FINAL` была последней непустой строкой ответа; добавлена регрессионная проверка текста после `FINAL`.
- LLM-сравнение больше не доверяет сохранённому итогу валидации и повторно проверяет фактический финальный ответ перед формированием промпта методолога.
- Успешно выполнены 22 unit-теста, компиляция Python-файлов, проверка `uv.lock` и `git diff --check`; внешние API-вызовы не выполнялись.

## 02.09.2026 16:30

- Верхнеуровневая CLI-справка дня 3 обновлена: описание `compare` учитывает опциональный LLM-анализ, а в примерах явно показана команда `compare --with-llm` с предупреждением о дополнительном API-вызове.
- Добавлен регрессионный тест основной справки; успешно выполнены 20 unit-тестов и компиляция Python-файлов. Внешние API-вызовы не выполнялись.

## 02.09.2026 16:25

- В день 3 добавлен опциональный `compare --with-llm`: отдельная системная роль получает полные сохранённые промпты, ответы, метрики и итоги локальной проверки всех четырёх стратегий.
- Детерминированный валидатор сохранён источником истины; содержимое ответов передаётся как недоверенные данные, а в консоли повторно не печатается.
- Выполнен реальный LLM-анализ последних результатов: подтверждена равная точность и преимущество прямого способа по эффективности; дополнительное сравнение использовало 3005 токенов.
- Добавлены проверки отдельного system-сообщения, полноты передаваемых данных, компактности консоли и локального сохранения анализа; unit-набор расширен до 19 тестов.

## 02.09.2026 16:16

- Консольный вывод дня 3 сокращён для записи видео: удалено повторное отображение исходной задачи и сообщения ожидания, заголовки стали однострочными.
- Метаданные и успешная автоматическая проверка теперь выводятся по одной строке; подробности отдельных проверок сохраняются только для ошибок.
- Добавлен тест отсутствия дублирования задачи в консоли; успешно выполнены 17 unit-тестов, компиляция, локальное сравнение сохранённых результатов и `git diff --check`. Повторные API-вызовы не выполнялись.

## 02.09.2026 14:55

- День 3 оформлен как самостоятельный `uv`-проект с четырьмя способами решения одной задачи: прямым, пошаговым, через созданный моделью промпт и через группу экспертов.
- Добавлены безопасная AST-проверка выражения, локальное сохранение результатов, сравнение токенов без повторного API-вызова и защита полного запуска явным подтверждением пяти запросов.
- Выполнены 16 локальных unit-тестов и реальный эксперимент через `deepseek-v4-flash`: все четыре способа дали корректный ответ; суммарный расход составил 105, 374, 855 и 803 токена соответственно.
- В README зафиксированы фактические результаты, честный вывод о равной точности одного запуска и сценарий покадровой записи видео; API-ключ и локальные JSON-результаты в Git не добавляются.

## 01.09.2026 17:34

- Для контролируемых ответов добавлена автоматическая валидация Markdown-структуры, числа ингредиентов и шагов, токенов и причины завершения; нарушение контракта теперь приводит к ненулевому коду выхода.
- Добавлен ограниченный режим серии `--mode controlled --runs N` с итоговой статистикой; допустимо от 1 до 20 вызовов, повтор недоступен для `free` и `compare`.
- Unit-набор расширен до девяти тестов: проверены успешный ответ, лишние ингредиенты, обрыв по лимиту и границы числа повторов; компиляция и `git diff --check` прошли.
- Выполнена реальная серия из десяти запросов DeepSeek: валидацию прошли 10/10 ответов, длина составила 162–197 токенов из 220, каждый ответ завершился с `finish_reason=stop`.

## 01.09.2026 17:18

- В вывод контролируемого режима добавлено явное пояснение: `<END>` является служебной stop-последовательностью и поэтому не входит в возвращаемый API текст.
- Документация второго дня перестроена вокруг фактического сценария задания: сравнение режимов, работа `max_tokens` и `stop`, изолированное окружение, проверки и план записи результата.
- Добавлено предупреждение против `uv sync --active` из окружения другого дня и тест консольного пояснения о stop-последовательности.
- Успешно выполнены пять локальных unit-тестов, компиляция Python-файлов и `git diff --check`; повторный внешний API-вызов не выполнялся.

## 01.09.2026 16:34

- Второй день оформлен как самостоятельный `uv`-проект: добавлены отдельные CLI-клиент, зависимости, lock-файл, шаблон `.env` и инструкция по запуску.
- Один пользовательский запрос теперь можно выполнить в режимах `free`, `controlled` и `compare`; в контролируемом режиме одновременно задаются фиксированный Markdown-формат, `max_tokens=220` и `stop=["<END>"]`.
- В вывод добавлены настройки режима, число сгенерированных токенов и причина завершения, чтобы различия двух API-вызовов были видны в записи результата.
- Добавлены четыре локальных теста контракта запросов и разбора ответа; успешно выполнены `uv run python -m unittest -v`, компиляция и проверка CLI-справки. Внешний DeepSeek API не вызывался.

## 31.08.2026 18:15

- Первый день приведён к завершённой структуре: в `README.md` явно разделены задание, решение, запуск, результат и формат сдачи.
- Для записи результата зафиксировано локальное имя `resources/day-01-result.mp4`; добавлен `.gitkeep`, чтобы пустой каталог ресурсов сохранялся в Git без самих медиафайлов.
- Локально проверены согласованность `uv.lock`, компиляция и CLI-сценарий с подменённым API-ответом; внешний DeepSeek API не вызывался.
- Запись результата проверена через `ffprobe`: корректный MP4/H.264, 1636x1168, длительность 8,42 секунды; визуальная проверка и проверка содержания видео не выполнялись.

## 31.08.2026 17:32

- Для каждого дня закреплён единый каталог `resources/` без преждевременного разделения на типы материалов.
- Имеющиеся видео дней 0 и 1 перенесены из корней заданий в `resources/`; `README.md` оставлен каноническим описанием задания.
- Каталог ресурсов добавлен в общие правила проекта и корневую документацию, а всё его локальное содержимое исключено из Git.
- Проверены новая структура, правила игнорирования и отсутствие изменений в коде дня 1.

## 31.08.2026 17:19

- Добавлен корневой `AGENTS.md` с постоянными правилами проекта: изоляция дней, ведение истории, защита секретов и обязательное использование `uv` для Python.
- Первый день преобразован в самостоятельный `uv`-проект: добавлен `pyproject.toml`, а `requirements.txt` удалён.
- Документация переведена на команды `uv sync` и `uv run`; ручное создание и активация виртуального окружения больше не требуется.
- Сгенерирован и проверен `uv.lock`; код запущен через `uv` без внешнего API-вызова.

## 31.08.2026 17:13

- В задании первого дня подключён `python-dotenv`: ключ DeepSeek теперь автоматически загружается из `day-01/.env` при обычном запуске программы.
- Добавлен фиксированный список зависимостей и инструкция по созданию виртуального окружения; требование к Python обновлено до версии 3.10+.
- Путь к `.env` привязан к каталогу скрипта, поэтому запуск работает как из `day-01`, так и из корня репозитория; системные переменные сохраняют приоритет.
- Проверены загрузка тестового значения из `.env`, отсутствие переопределения системной переменной, компиляция и обработка ответа без реального API-вызова.

## 31.08.2026 17:05

- Выполнено задание первого дня: добавлен минимальный Python CLI-клиент для `deepseek-v4-flash` через официальный Chat Completions endpoint.
- API-ключ вынесен в `DEEPSEEK_API_KEY`, добавлен безопасный `.env.example`; сетевые и API-ошибки обрабатываются без вывода ключа.
- Инструкция дня дополнена подготовкой окружения, командами запуска и описанием API-вызова.
- Выполнены статическая проверка Python-кода и локальные проверки сценариев без ключа; реальный API-вызов без пользовательского ключа не выполнялся.

## 31.08.2026 15:45

- Подготовлен единый репозиторий для всего AI Advent Challenge.
- Каталоги дней приведены к единому формату `day-XX`, описание первого задания перенесено в `day-01/README.md`.
- Добавлены корневое оглавление и `.gitignore`; экспорт чатов, видео и локальные секреты исключены из Git.
- Проверена итоговая файловая структура и правила игнорирования.
