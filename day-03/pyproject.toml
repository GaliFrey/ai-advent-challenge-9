[project]
name = "ai-advent-day-03"
version = "0.1.0"
description = "Сравнение четырёх способов рассуждения через DeepSeek API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv==1.2.3",
]

[tool.uv]
package = false
