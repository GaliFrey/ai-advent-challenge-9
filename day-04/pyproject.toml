[project]
name = "ai-advent-day-04"
version = "0.1.0"
description = "Два эксперимента с температурой LLM и сохраняемыми результатами"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv==1.2.3",
]

[tool.uv]
package = false
